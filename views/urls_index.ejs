<!DOCTYPE html>
<html lang="en">
<head>
  <%- include ./partials/_header.ejs %>
  <title>TinyApp</title>
</head>
<body style="text-align:center;">

  <div>
    <% for(url in urls) { %>
        <a href="<%= urls[url] %>"><%= urls[url] %></a> --> <%= url %>
        <a href="/urls/<%= url %>">EDIT</a>
        <form method="POST" action="/urls/<%= url %>/delete"><button>DELETE</button></form>

    <% }; %>
  </div>

  <button href="/urls/new" >Shorten URL!</button>

</body>
</html>