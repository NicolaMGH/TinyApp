<!DOCTYPE html>
<html lang="en">
<head>
  <%- include ./partials/_header.ejs %>
  <title>TinyApp</title>
</head>
<body>

  <ul>
    <% for(url in urls) { %>
        <li><a href="<%= urls[url] %>"><%= urls[url] %></a> --> <%= url %> </li>
        <form method="POST" action="/urls/<%= url %>/delete"><button>DELETE</button></form>

    <% }; %>
  </ul>

  <button href="/urls/new" >Shorten URL!</button>

</body>
</html>